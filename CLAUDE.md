# seb-hands-on

Sapporo Engineer Base のハンズオンイベント用ガイド集です。
プログラミング未経験者・初学者が、1日で自分のブログを作ってインターネットに公開するまでを扱います。

## ディレクトリ

| パス | 内容 |
|------|------|
| `guides/` | ハンズオンガイド本体（README + 01〜08） |
| `scripts/check-guides.py` | ガイドの記述規約チェック |
| `blog-app/` | 動作確認用に生成したアプリ（gitignore 済み・別リポジトリで管理） |

## ガイド執筆ルール

`guides/` 配下の Markdown を書くときは、以下を必ず守ってください。

### 1. 太字は使用しない

`**...**` と `<b>` / `<strong>` はガイド内で禁止です。
強調が必要な場合は、以下で代替してください。

| 代わりに使うもの | 用途 |
|----------------|------|
| 見出し（`##` `###` `####`） | 話題の区切り |
| 引用ブロック（`> ⚠️` `> 💡` `> メンター視点`） | 注意喚起・補足・メンター向けの補足 |
| 表 | 対比・一覧 |
| 絵文字 | 視線の誘導 |
| 箇条書き | 並列する要素 |

コードブロック内は対象外です（Markdown の書き方を説明するサンプルなどは `**` を含んでよい）。

### 2. 図は Mermaid で表現する

アスキーアートで図を描かないでください。
ターミナルの実行結果やURLの例は「図」ではないので、コードブロックのままで構いません。

### 3. H1 は1ファイルに1つ

ページタイトルのみ `#` を使い、以降は `##` から始めてください。

### 4. 文体

- 親しみやすい、です・ます調
- 端的で分かりやすい表現（1文が長くなりすぎないように）
- 初学者が知らない用語は、初出時に一言で説明する

### 5. メンター視点を書く

テキストだけでは伝わりにくい箇所には、メンターが口頭で補足できるよう
`> メンター視点` の引用ブロックで観点を書いてください。

### 6. バージョンを明示する

ガイドが前提とするバージョン（Laravel / PHP / Node.js など）を明示し、
古い記事に引きずられないよう注意書きを添えてください。

## チェック

ガイドを編集したら、コミット前に必ず実行してください。

```bash
python3 scripts/check-guides.py
```

太字の混入、H1 の重複、コードブロックの閉じ忘れ、リンク切れを検出します。
