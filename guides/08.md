# 発展課題に挑戦しよう

日記アプリが完成したあなたへ。さらにスキルアップしたい方向けの発展課題を用意しました。

自分のペースで挑戦してみてください！

## この課題について

発展課題は、ハンズオンで学んだ内容を応用するものです。

- **中級**: 今回の知識で挑戦できる課題
- **上級**: 新しい概念を学びながら挑戦する課題

すべてを完了する必要はありません。興味のある課題から取り組んでみましょう。

> **メンター視点**
> - 発展課題は自習用です。ハンズオン時間内に終わらなくても問題ありません
> - 詰まったときは、公式ドキュメントや検索を活用することも学習の一部です
> - 完成度より「挑戦すること」を大切にしてください


## 中級課題

今回学んだ内容の応用で取り組める課題です。


### 課題1: 日記にタイトルを追加する

日記に「タイトル」欄を追加して、一覧画面でタイトルを表示できるようにしましょう。

#### やること

1. マイグレーションを作成して、`diaries` テーブルに `title` カラムを追加
2. `Diary` モデルの `$fillable` に `title` を追加
3. 新規作成・編集画面にタイトル入力欄を追加
4. 一覧画面でタイトルを表示

#### ヒント

- 新しいマイグレーションを作成するコマンド: `php artisan make:migration add_title_to_diaries_table`
- マイグレーションで `$table->string('title')->nullable();` を追加
- `nullable()` をつけると、タイトルなしでも保存できます

> **メンター視点**
> - この課題は、04.md で学んだ「マイグレーション → モデル → ビュー」の流れを復習できます
> - 既存のテーブルにカラムを追加する方法を学べます

### 課題2: 天気を記録する

日記にその日の天気を記録できるようにしましょう。

#### やること

1. `diaries` テーブルに `weather` カラムを追加
2. 新規作成・編集画面に天気の選択欄（セレクトボックス）を追加
3. 一覧画面で天気アイコンを表示

#### 天気の選択肢の例

```
晴れ ☀️
曇り ☁️
雨 🌧️
雪 ❄️
```

#### ヒント

- HTMLのセレクトボックス: `<select name="weather">...</select>`
- 選択肢は `<option value="sunny">晴れ ☀️</option>` のように書きます
- 絵文字はそのままコードに書けます

> **メンター視点**
> - セレクトボックスの使い方を学べます
> - 決まった選択肢から選ばせるUIパターンです

### 課題3: キーワード検索を追加する

日記の内容をキーワードで検索できるようにしましょう。

#### やること

1. 一覧画面に検索フォームを追加
2. コントローラーで検索キーワードを受け取る
3. キーワードを含む日記だけを表示

#### ヒント

- 検索フォームは `<form method="GET">` で作成
- コントローラーで `$request->query('keyword')` でキーワードを取得
- Eloquentの `where('content', 'like', "%{$keyword}%")` で部分一致検索

#### 参考: 検索のイメージ

```
検索キーワード: [ラーメン    ] [検索]

→ 「ラーメン」を含む日記だけが表示される
```

> **メンター視点**
> - GETパラメータの扱い方を学べます
> - SQLのLIKE検索の概念を説明すると良いでしょう

### 課題4: お気に入り機能を追加する

日記にお気に入り（スター）をつけられるようにしましょう。

#### やること

1. `diaries` テーブルに `is_favorite` カラム（boolean型）を追加
2. 一覧画面にスターアイコンを表示
3. スターをクリックしたらお気に入りを切り替え
4. お気に入りだけを表示するフィルター機能

#### ヒント

- `$table->boolean('is_favorite')->default(false);`
- スターアイコン: ⭐（お気に入り）、☆（通常）
- お気に入りの切り替えは、新しいルートとコントローラーメソッドが必要

> **メンター視点**
> - boolean型の使い方を学べます
> - トグル（ON/OFF切り替え）のUIパターンです

### 課題5: 日記の並べ替え機能を追加する

日記の表示順を切り替えられるようにしましょう。

#### 並べ替えの選択肢

- 日付が新しい順（デフォルト）
- 日付が古い順
- 作成日が新しい順
- 作成日が古い順

#### ヒント

- 並べ替えオプションをセレクトボックスで選択
- `orderBy('date', 'desc')` や `orderBy('created_at', 'asc')` で並べ替え
- URLパラメータで並べ替え条件を渡す

> **メンター視点**
> - ORDER BY の概念を学べます
> - `date` と `created_at` の違いを説明すると良いでしょう

## 上級課題

新しい概念を学びながら挑戦する課題です。

### 課題6: タグ機能を追加する

日記にタグをつけて分類できるようにしましょう。

#### 概要

```
今日の日記
タグ: #仕事 #ミーティング #アイデア
```

#### 学ぶこと

- **多対多リレーション**: 1つの日記に複数のタグ、1つのタグに複数の日記
- **中間テーブル**: `diary_tag` テーブルで関連を管理
- **belongsToMany**: Eloquentの多対多リレーション

#### 必要なテーブル

```
tags テーブル
- id
- name

diary_tag テーブル（中間テーブル）
- diary_id
- tag_id
```

#### 参考リンク

- Laravel公式: [Eloquent リレーション - 多対多](https://readouble.com/laravel/11.x/ja/eloquent-relationships.html#many-to-many)

> **メンター視点**
> - データベース設計の概念を学べます
> - 多対多リレーションは実務でもよく使うパターンです

### 課題7: 画像添付機能を追加する

日記に写真を添付できるようにしましょう。

#### 概要

- 新規作成・編集画面で画像をアップロード
- 日記と一緒に画像を表示
- 画像は `storage/app/public` に保存

#### 学ぶこと

- **ファイルアップロード**: `$request->file('image')`
- **ストレージ**: Laravelのファイル保存機能
- **シンボリックリンク**: `php artisan storage:link`

#### ヒント

- フォームに `enctype="multipart/form-data"` が必要
- `$request->file('image')->store('diaries', 'public')` で保存
- 画像パスをデータベースに保存

#### 参考リンク

- Laravel公式: [ファイルストレージ](https://readouble.com/laravel/11.x/ja/filesystem.html)

> **メンター視点**
> - ファイルアップロードはWebアプリの基本機能です
> - セキュリティ（ファイルタイプの検証）についても触れると良いでしょう

### 課題8: ログイン機能を追加する

自分だけが見られる日記帳にしましょう。

#### 概要

- ユーザー登録・ログイン・ログアウト機能
- ログインしないと日記が見られない
- 自分の日記だけが表示される

#### 学ぶこと

- **認証（Authentication）**: ユーザーの識別
- **Laravel スターターキット**: 認証機能を含むプロジェクトテンプレート
- **ミドルウェア**: ログイン必須のルート設定

#### Laravel 12 のスターターキット

Laravel 12では、認証機能を含む新しいスターターキットが用意されています。

| キット | 特徴 |
|--------|------|
| **Livewire** | PHPのみで動的UI。Bladeベースで学習しやすい |
| **React** | React + TypeScript + Inertia。SPA向け |
| **Vue** | Vue + TypeScript + Inertia。SPA向け |

#### 新規プロジェクトで始める場合

```bash
laravel new my-app
# 対話形式でスターターキットを選択（Livewire推奨）

cd my-app
npm install && npm run build
php artisan migrate
```

#### 既存プロジェクトに認証を追加する場合（Fortify + Blade）

以下の手順で、既存の日記アプリにログイン機能を追加できます。

##### Step 1: Fortifyのインストール

```bash
composer require laravel/fortify
php artisan fortify:install
php artisan migrate
```

##### Step 2: FortifyServiceProviderの登録

`bootstrap/providers.php` に以下を追加：

```php
return [
    App\Providers\AppServiceProvider::class,
    App\Providers\FortifyServiceProvider::class,  // 追加
];
```

##### Step 3: ビューの設定

`app/Providers/FortifyServiceProvider.php` の `boot()` メソッドに追加：

```php
use Laravel\Fortify\Fortify;

public function boot(): void
{
    // ログイン画面
    Fortify::loginView(function () {
        return view('auth.login');
    });

    // ユーザー登録画面
    Fortify::registerView(function () {
        return view('auth.register');
    });
}
```

##### Step 4: ログイン画面の作成

`resources/views/auth/login.blade.php` を作成：

```blade
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ログイン - 日記アプリ</title>
    @vite(['resources/css/app.css'])
</head>
<body>
    <div class="container">
        <h1>ログイン</h1>

        @if ($errors->any())
            <div class="error-message">
                <ul>
                    @foreach ($errors->all() as $error)
                        <li>{{ $error }}</li>
                    @endforeach
                </ul>
            </div>
        @endif

        <form method="POST" action="{{ route('login') }}">
            @csrf

            <div class="form-group">
                <label for="email">メールアドレス</label>
                <input type="email" id="email" name="email" value="{{ old('email') }}" required>
            </div>

            <div class="form-group">
                <label for="password">パスワード</label>
                <input type="password" id="password" name="password" required>
            </div>

            <div class="form-group">
                <label>
                    <input type="checkbox" name="remember"> ログイン状態を保持する
                </label>
            </div>

            <button type="submit" class="btn">ログイン</button>
        </form>

        <p><a href="{{ route('register') }}">アカウントを作成する</a></p>
    </div>
</body>
</html>
```

##### Step 5: ユーザー登録画面の作成

`resources/views/auth/register.blade.php` を作成：

```blade
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ユーザー登録 - 日記アプリ</title>
    @vite(['resources/css/app.css'])
</head>
<body>
    <div class="container">
        <h1>ユーザー登録</h1>

        @if ($errors->any())
            <div class="error-message">
                <ul>
                    @foreach ($errors->all() as $error)
                        <li>{{ $error }}</li>
                    @endforeach
                </ul>
            </div>
        @endif

        <form method="POST" action="{{ route('register') }}">
            @csrf

            <div class="form-group">
                <label for="name">名前</label>
                <input type="text" id="name" name="name" value="{{ old('name') }}" required>
            </div>

            <div class="form-group">
                <label for="email">メールアドレス</label>
                <input type="email" id="email" name="email" value="{{ old('email') }}" required>
            </div>

            <div class="form-group">
                <label for="password">パスワード</label>
                <input type="password" id="password" name="password" required>
            </div>

            <div class="form-group">
                <label for="password_confirmation">パスワード（確認）</label>
                <input type="password" id="password_confirmation" name="password_confirmation" required>
            </div>

            <button type="submit" class="btn">登録する</button>
        </form>

        <p><a href="{{ route('login') }}">ログインはこちら</a></p>
    </div>
</body>
</html>
```

##### Step 6: ルートの保護

`routes/web.php` を編集して、認証が必要なルートを保護：

```php
use App\Http\Controllers\DiaryController;
use Illuminate\Support\Facades\Route;

// ログインページ（ゲスト用）
Route::get('/login', function () {
    return view('auth.login');
})->middleware('guest')->name('login');

// 認証が必要なルート
Route::middleware('auth')->group(function () {
    Route::get('/', [DiaryController::class, 'index'])->name('diaries.index');
    Route::get('/diaries/create', [DiaryController::class, 'create'])->name('diaries.create');
    Route::post('/diaries', [DiaryController::class, 'store'])->name('diaries.store');
    Route::get('/diaries/{diary}/edit', [DiaryController::class, 'edit'])->name('diaries.edit');
    Route::put('/diaries/{diary}', [DiaryController::class, 'update'])->name('diaries.update');
    Route::delete('/diaries/{diary}', [DiaryController::class, 'destroy'])->name('diaries.destroy');

    // ログアウト
    Route::post('/logout', function () {
        auth()->logout();
        return redirect('/login');
    })->name('logout');
});
```

##### Step 7: ログアウトボタンの追加

日記一覧画面（`resources/views/diaries/index.blade.php`）にログアウトボタンを追加：

```blade
<form method="POST" action="{{ route('logout') }}" style="display: inline;">
    @csrf
    <button type="submit">ログアウト</button>
</form>
```

##### Step 8: 自分の日記だけ表示する（オプション）

日記を特定のユーザーに紐づけるには：

1. `diaries` テーブルに `user_id` カラムを追加
2. `Diary` モデルに `user()` リレーションを追加
3. コントローラーで `auth()->id()` を使ってフィルタリング

#### 参考リンク

- Laravel公式: [Starter Kits](https://laravel.com/docs/12.x/starter-kits)
- Laravel公式: [Fortify](https://laravel.com/docs/12.x/fortify)

> **メンター視点**
> - 認証は多くのWebアプリで必須の機能です
> - 初心者にはLivewireスターターキットが学習しやすいです（PHPのみ）
> - 既存プロジェクトへの追加は難易度が高いため、新規プロジェクトで試すことを推奨します

### 課題9: Markdown対応にする

日記の内容をMarkdown記法で書けるようにしましょう。

#### 概要

```markdown
# 今日のハイライト

- ランチに **美味しいパスタ** を食べた
- 新しい本を読み始めた

> 明日も頑張ろう！
```

↓ 表示時はHTMLに変換

#### 学ぶこと

- **Composerパッケージ**: 外部ライブラリの導入
- **Markdownパーサー**: テキストをHTMLに変換
- **XSS対策**: HTMLのサニタイズ

#### おすすめライブラリ

- `league/commonmark`: PHP用Markdownパーサー

```bash
composer require league/commonmark
```

> **メンター視点**
> - 外部ライブラリを導入する流れを学べます
> - セキュリティ（XSS）についても重要な学びがあります

## 課題に取り組むときのアドバイス

### 1. 小さく始める

一度に全部作ろうとせず、小さな部分から始めましょう。

```
❌ いきなり完璧なタグ機能を作ろうとする
✅ まずタグのテーブルだけ作ってみる
```

### 2. エラーを恐れない

エラーは「何が問題か教えてくれるメッセージ」です。

```
エラーが出た
    ↓
エラーメッセージを読む
    ↓
検索してみる
    ↓
解決！（これが一番の学び）
```

### 3. 動いたらコミット

小さな単位で動作確認し、動いたらGitでコミットしましょう。

```bash
git add .
git commit -m "タイトル機能を追加"
```

こうすると、失敗しても戻れます。

### 4. 調べ方を覚える

プログラミングは「調べながら作る」のが普通です。

- 公式ドキュメントを読む
- エラーメッセージで検索する
- 動くコードを参考にする

## おすすめの進め方

### 初めての方

1. **課題1: タイトルを追加** - 今回の復習
2. **課題2: 天気を記録** - 新しいUIパターン
3. **課題3: キーワード検索** - 実用的で達成感がある

### もっと挑戦したい方

4. **課題8: ログイン機能** - 実用的なアプリに近づく
5. **課題6: タグ機能** - データベース設計を学ぶ

## 困ったときは

- **公式ドキュメント**: [Laravel 日本語ドキュメント](https://readouble.com/laravel/)
- **検索**: エラーメッセージをそのまま検索
- **質問**: メンターや勉強会で質問

一人で抱え込まず、どんどん質問してください。質問することも大切なスキルです！

**最後に**
ここまで読んでくださりありがとうございます。
プログラミングは、作れば作るほど楽しくなります。この日記アプリを自分だけのオリジナルアプリに育てていってください。
